# Deployment validation template

Copy the CSV outside Git and add only consented, pseudonymous records. Use `calibration` and `test` identity-disjoint splits. Never add employee names, raw biometric files, or completed manifests to this repository.
